# Local preview. Keep localhost consistent with the backend return URL allowlist.
PORT=4173
MORI_POC_ORIGIN=http://localhost:4173
MORI_API_BASE_URL=http://localhost:8000
# Social client IDs/secrets belong in the backend environment, never here.
