# Mori 프로젝트 설계 초안

작성일: 2026-09-16 · 관리 브랜치: `plan` · 상태: 사용자 의도 검토 전

## 1. 만들려는 서비스

**말하거나 버튼을 누르면 필요한 일을 처리하고, 반복되는 생활 패턴을 기억해 적절한 때 먼저 보여주는 가벼운 개인 비서.**

핵심은 사용자가 매번 질문을 잘 작성하는 것이 아니라, 자신의 일정과 생활 정보를 쉽게 맡기고 필요할 때 바로 꺼내 보는 경험이다. 첫 화면은 대시보드이며 일반 대화도 지원한다. 부모님처럼 복잡한 앱에 익숙하지 않은 사람도 사용할 수 있어야 한다.

여기서 ‘가볍다’는 세 가지를 의미한다. 화면과 조작이 단순하고, 간단한 조회가 빠르며, 개인 프로젝트로 운영 가능한 비용과 복잡도를 유지한다.

## 2. 확정 요구사항과 설계 제안

### 사용자가 제시한 요구사항

| 구분 | 요구사항 |
| --- | --- |
| 개발 역할 | 실제 서비스 프론트엔드는 사용자가 전담한다. 백엔드와 인프라는 AI가 개발한다. |
| 브랜치 | `master`: 실제 제품 개발, `poc`: 요청한 PoC 화면 개발, `plan`: 설계와 작업 계획 관리 |
| 기본 사용자 | 공용 Hermes Agent 실행 자원을 이용한다. |
| 유료 사용자 | 사용자별 전용 Hermes Pod를 제공하고 CPU·메모리를 제한한다. |
| 주력 경험 | 음성 입력, 일정 관리, 반복 작업의 기억과 자동화, 개인 비서 기능 |
| 개인화 | 반복 사용에서 스킬을 스스로 만들고 저장해 재사용한다. |
| 첫 화면 | 채팅 목록 대신 개인화된 대시보드와 바로 실행하는 버튼을 제공한다. |
| 휴대폰 위젯 | 시간과 사용자 상황에 맞는 정보를 보여준다. |
| 확장 기능 | 일반 채팅, 인터넷 검색, 여행 일정 작성, Excel·PDF 결과물 제공 |
| 모델 운영 | 보유한 RTX 3090에서 Qwen3.8 계열 모델을 로컬로 구동한다. |

### 이 초안에서 추가한 제안 — 확정 사항 아님

1. **무료 사용자도 기억·일정·스킬·파일은 개인별로 분리한다.** ‘공용’은 실행 자원 공유로 해석한다.
2. **유료 전용 Pod와 GPU 모델 서버는 분리한다.** 개인별 Hermes는 CPU·메모리를 사용하고, 모든 Hermes가 공용 RTX 3090 추론 서버를 호출한다.
3. **일정과 반복 실행의 기준 데이터는 Mori 백엔드가 관리한다.** Hermes는 이해·계획·스킬 생성·도구 호출을 담당한다.
4. **스킬 생성과 저장은 자동으로 한다.** 실행은 사용자가 맡긴 범위 안에서 자동화하고, 새로운 외부 권한이나 비용이 필요한 경우에만 별도 동의를 받는다.
5. **서비스 내부 캘린더를 먼저 구현한다.** Google Calendar·Apple Calendar 등의 연동 범위는 따로 결정한다.
6. **PoC는 모바일 화면 중심으로 검증한다.** 실제 앱 기술과 Android/iOS 우선순위는 사용자가 선택한다.

## 3. 설계 문서 구성

| 문서 | 범위 | 주요 담당 |
| --- | --- | --- |
| [architecture/plan.md](architecture/plan.md) | 시스템 구조, 공용/전용 실행, 모델 서버, 스케줄링, 배포와 운영 | AI, 사용자 검토 |
| [front/plan.md](front/plan.md) | 대시보드, 버튼·음성 흐름, 개인화, 캘린더, 위젯, PoC 범위 | 실제 개발: 사용자 / 요청한 PoC: AI |
| [back/plan.md](back/plan.md) | 도메인·데이터, Hermes 연결, 도구 권한, API, 자동화, 검증 기준 | AI |

이 파일을 전체 범위와 작업 상태의 기준으로 삼는다. 상세 구현 제안은 각 문서에서 관리하며, 결정이 바뀌면 관련 문서와 작업 상태를 함께 갱신한다. 현재 문서는 설계이며 기능이 구현된 상태를 뜻하지 않는다.

## 4. 핵심 사용 시나리오

### 4.1 주차 위치를 기억하고 출근할 때 보여주기

1. 사용자가 ‘주차 기억하기’를 누르거나 마이크에 “오늘 지하 3층 B12에 주차했어”라고 말한다.
2. 비서는 위치 표현과 기록 시간을 저장하고 “지하 3층 B12, 기억했어요”라는 결과 카드를 보여준다.
3. “차 어디에 뒀지?”라고 묻거나 버튼을 누르면 가장 최근 유효한 기록을 바로 보여준다.
4. 사용자가 “평일 아침 7시 30분에 보여줘”라고 하면 반복 규칙을 저장한다.
5. 반복 기록과 조회 패턴이 쌓이면 ‘주차 기억 → 최신 기록 조회 → 출근 시간에 표시’ 절차를 개인 스킬로 자동 생성·저장한다.
6. 자동 개인화를 켠 사용자는 허용된 범위에서 아침 대시보드와 휴대폰 위젯에 주차 정보를 받는다. 사용자는 시간 수정, 숨기기, 자동화 중지를 할 수 있다.

초기에는 사용자가 말한 층·구역·번호가 기준이다. 지하 주차장의 정확한 실내 위치를 GPS로 알아내거나, 사용자가 알려주지 않은 위치를 추측하는 기능은 전제하지 않는다. 예전 기록에는 기록 시각을 표시한다.

### 4.2 말로 일정 등록하기

“이번 주 토요일 오후 6시에 강남역에서 민수랑 저녁 약속 있어”라고 하면 제목·날짜·시간·장소를 추출해 캘린더에 등록한다. 필수 정보가 명확하면 바로 처리하고 결과 카드에서 수정하거나 취소할 수 있게 한다. “토요일 저녁쯤”처럼 시간이 모호하면 필요한 정보만 짧게 확인한다.

등록한 일정은 오늘 대시보드, 캘린더, 예정된 알림에 반영한다. 기본 알림 시간과 일정 길이는 사용자의 설정을 따르며 초안에서 임의로 확정하지 않는다.

### 4.3 여행 계획과 파일 받기

‘여행 계획’을 누르고 기간·지역·동행·관심사·예산 등 필요한 조건을 전달한다. 비서가 인터넷에서 정보를 찾아 일자별 일정과 이동 계획을 정리하고, 앱에서 확인할 수 있는 결과와 `.xlsx`·`.pdf` 파일을 제공한다. 출처와 확인 시점을 함께 남긴다. 예약·결제·타인에게 보내기는 초기 범위에 포함하지 않는다.

### 4.4 일반 대화

버튼으로 처리하기 어려운 질문은 일반 채팅에서 이어간다. 필요한 경우 대화 결과를 일정·기억·자동화 카드로 전환한다. 이전 대화 목록은 보조 화면에 둔다.

## 5. 기능 범위와 구현 순서 제안

단계 순서는 검토용 제안이다. 특히 개인화와 스킬 학습은 핵심 기능이며, 뒤로 미루는 부가 기능으로 취급하지 않는다.

| 단계 | 목표 | 포함 기능 | 완료 기준 |
| --- | --- | --- | --- |
| P0 설계 합의 | 사용자 의도 맞추기 | 이 문서, 역할·플랫폼·자동화 범위 결정 | 아래 검토 항목의 방향 확정 |
| P1 화면 PoC + 기술 검증 | UX와 기술 가능성 확인 | `poc`의 요청된 화면, 모델 도구 호출, Hermes 사용자 격리, 위젯 가능성 확인 | 주차·일정 시연, 3090 측정 결과, 격리 방식 결정 |
| P2 개인 사용 알파 | 매일 쓸 수 있는 핵심 기능 | 음성·텍스트 입력, 주차 기록, 내부 캘린더, 예약 실행, 기본 채팅, 대시보드, 제한된 개인 스킬 자동 저장·재사용 | 앱/에이전트 재시작 후에도 기록·일정·자동화 유지 |
| P3 소규모 베타 | 실제 모바일 비서 경험 검증 | 네이티브 위젯·알림, 개인화 조절, 검색과 여행 계획, Excel·PDF, 무료 사용자 간 격리 검증 | 실제 휴대폰과 여러 계정에서 전체 흐름 검증 |
| P4 유료 제공 | 개인 전용 실행 환경 제공 | 사용자별 Pod·볼륨, 자원 한도, 요금제 상태, 생성·복구·해지 처리 | 전용 Pod 재생성·등급 변경에도 데이터와 일정 보존 |

외부 캘린더 동기화는 별도 범위 합의 후 적절한 단계에 넣는다. 결제 서비스, 가격, 무료/유료 기능별 차등은 아직 정하지 않았다. 이 초안의 유료 차별점은 전용 실행 환경이며, 기능 제한을 새로 확정하지 않는다.

## 6. 브랜치와 협업 방식

| 브랜치 | 담는 내용 | 운영 원칙 |
| --- | --- | --- |
| `master` | 실제 서비스 프론트·백엔드·인프라와 검증된 변경 | 제품 개발의 기준. 프론트 변경은 사용자 담당. AI는 요청받은 백엔드·인프라를 개발한다. |
| `poc` | 사용자가 요청한 화면 실험, 목업 데이터, 상호작용 시연 | PoC 범위를 명시하고 실제 연동 여부를 구분한다. |
| `plan` | `plan.md`, 세 영역의 설계·작업 상태·결정 기록 | 작업 시작 전에 범위와 담당을 정리하고 결과를 갱신한다. |

작업 흐름은 `plan에서 범위 정리 → 필요하면 poc에서 화면 검증 → master에서 실제 구현 → plan에 결과 반영`이다. 장기 브랜치 세 개를 자동으로 순서대로 병합한다는 뜻은 아니다. PoC를 제품에 반영할 때는 사용자가 검토한 내용만 이식하고, 계획 변경은 `plan`에 기록한다.

필요한 경우에만 `master`에서 짧은 작업 브랜치를 만들어 검증 후 합친다. 이는 보조 방식이며, 사용자가 정한 세 브랜치의 역할을 대체하지 않는다. 화면·API 타입·모바일 위젯 UI도 실제 제품에서는 프론트 담당 범위다. AI는 API 계약, 예시 응답, 오류 상태와 연동 문서를 제공한다.

현재 저장소에는 기존 코드와 커밋이 없다. 이번 작업은 `plan`의 문서 초기화만 수행하며, `master`와 `poc`의 실제 개발은 별도 작업으로 시작한다.

## 7. 작업 목록

상태는 `제안 → 합의 → 진행 → 검증 → 완료`로 관리한다. 완료 표시에는 구현 커밋 또는 검증 근거를 붙인다.

| ID | 영역 | 작업 | 담당 | 선행 조건 | 상태 |
| --- | --- | --- | --- | --- | --- |
| A-01 | Architecture | 출시 플랫폼, 호스트 사양, 운영 범위 확정 | 사용자 + AI | 문서 검토 | 제안 |
| A-02 | Architecture | Qwen3.8 세부 모델·양자화·추론 엔진을 3090에서 측정 | AI | A-01, 하드웨어 접근 | 제안 |
| A-03 | Architecture | 무료 공용 실행의 사용자 상태·도구 격리 검증 | AI | Hermes 버전 선정 | 제안 |
| F-01 | Front | 대시보드·음성·주차·일정 PoC | 요청 시 AI | P0 검토, 플랫폼 방향 | 제안 |
| F-02 | Front | 실제 화면·녹음·인증·API 연동 구현 | 사용자 | API 계약, F-01 검토 | 제안 |
| B-01 | Back | 인증, 사용자별 기록, 일정·도구 API | AI | API 계약 합의 | 제안 |
| B-02 | Back | Hermes Adapter, 로컬 LLM, STT 연결 | AI | A-02, A-03 | 제안 |
| B-03 | Back | 예약 실행, 중복 방지, 실패 복구, 알림 | AI | B-01 | 제안 |
| B-04 | Back | 반복 패턴 감지·개인 스킬 자동 저장·권한 내 실행 | AI | B-01~03 | 제안 |
| F-03 | Front | 실제 모바일 위젯·알림·개인화 설정 | 사용자 | 플랫폼 결정, 위젯 API | 제안 |
| B-05 | Back | 여행 검색·계획, Excel·PDF 생성 | AI | B-02, 파일 저장소 | 제안 |
| A-04 | Architecture | 유료 Pod·자원 한도·데이터 이전·복구 | AI | A-03, 자원 실측, 요금제 정책 | 제안 |

## 8. 사용자 의도와 맞는지 먼저 볼 항목

| 항목 | 현재 초안의 해석 | 아직 결정할 내용 |
| --- | --- | --- |
| ‘공용 에이전트’ | 실행 자원은 공용, 기억과 스킬은 사용자별 | 무료 사용자도 이 수준의 개인화를 제공하는지 |
| ‘개인용 에이전트’ | 사용자별 Hermes Pod와 저장 공간, LLM은 공용 | 유료 Pod 상시 실행 여부와 실제 등급 차이 |
| 자동 학습 | 스킬 생성·저장은 자동, 이미 허용한 범위에서 실행 | 자동 개인화의 초기 기본값과 외부 작업의 허용 범위 |
| 휴대폰 앱 | 모바일 사용과 실제 OS 위젯이 최종 목표 | Android 먼저인지, iOS도 처음부터인지, 프론트 기술 |
| 캘린더 | Mori 내부 캘린더를 우선 구현 | 처음부터 기존 캘린더 연동이 필수인지 |
| 로컬 모델 | Qwen3.8을 사용하되 세부 모델과 양자화는 실측 후 결정 | 염두에 둔 정확한 모델 ID와 서버 RAM·CPU·OS |
| 운영 규모 | 개인 알파 → 소수 사용자 베타 → 유료 제공 | 예상 사용자 수, 동시 사용, 24시간 서버 가동 가능 여부 |
| UI 개인화 | 필요한 카드가 올라오되 기본 버튼 위치는 안정적으로 유지 | 자동 배치 변화의 허용 정도 |

## 9. 설계 근거와 아직 검증하지 않은 것

공식 자료는 2026-09-16에 확인했다. Hermes에는 기억, 스킬, 예약 작업과 API 연결 기능이 있지만, Mori에 필요한 사용자 격리·권한·제품 UX는 별도 설계와 검증이 필요하다. [Hermes 공식 저장소](https://github.com/NousResearch/hermes-agent), [API와 사용자별 프로필](https://hermes-agent.nousresearch.com/docs/user-guide/features/api-server).

Qwen 공식 모델 카드에서 `Qwen/Qwen3.8-27B`를 확인했다. 이는 후보 확인이며 사용자 모델을 27B로 확정하거나 RTX 3090에서의 속도·동시 처리량을 검증한 것은 아니다. [Qwen3.8-27B 모델 카드](https://huggingface.co/Qwen/Qwen3.8-27B).

실제 GPU 실행, Hermes 설치, 모바일 위젯, 부하 테스트는 아직 수행하지 않았다. 상세 문서의 기술 조합과 수치 중 ‘제안’, ‘예시’, ‘목표’로 표시한 내용은 측정 결과가 아니다.
