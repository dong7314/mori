{
  "name": "mori-poc",
  "version": "0.2.0",
  "private": true,
  "description": "Mori UI/UX prototype with HTML, vanilla JavaScript and SCSS",
  "scripts": {
    "start": "node --env-file-if-exists=.env scripts/serve.mjs",
    "dev": "node --env-file-if-exists=.env scripts/serve.mjs",
    "build": "sass styles/main.scss styles/main.css --no-source-map --style=expanded",
    "styles:watch": "sass --watch styles/main.scss:styles/main.css --no-source-map",
    "check": "node --check src/app.js && node --check src/api.mjs && node --check src/ui-model.mjs && node --check scripts/server.mjs && node --check scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "sass": "^1.104.1"
  }
}
